'use client'

export function ScrollToTopOnRouteChange() {
  // Next.js App Router handles scroll restoration automatically.
  // We return null to keep the component in the tree but without any side effects.
  return null
}
